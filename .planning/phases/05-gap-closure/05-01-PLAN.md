---
phase: 05-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
gap_closure: true
---

<objective>
Add missing ICS data file and expand E2E test coverage for verification gaps.

Purpose: Close identified verification gaps by adding the required ICS data file and expanding E2E tests to cover CRUD operations and pace modal flows.
Output: ICS file in place + expanded test coverage
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/tests/e2e/week-planner.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ICS data file</name>
  <files>frontend/public/data/trainingsplan_v2.ics</files>
  <action>
Create the trainingsplan_v2.ics file in frontend/public/data/ with sample running workouts for testing.
Include varied workout types: easy runs, interval training, long runs, rest days, yoga.
Each VEVENT should have: DTSTART, DTEND, SUMMARY, DESCRIPTION (with distance for runs).
Use realistic schedule spanning 2-3 weeks for E2E test coverage.
  </action>
  <verify>
File exists at frontend/public/data/trainingsplan_v2.ics
File parses as valid ICS format
  </verify>
  <done>ICS file present with valid VEVENT entries</done>
</task>

<task type="auto">
  <name>Task 2: Expand E2E CRUD tests</name>
  <files>frontend/tests/e2e/week-planner.spec.ts</files>
  <action>
Expand E2E tests in week-planner.spec.ts to cover:

1. Create new workout via UI:
   - Click "+" button to open create modal
   - Fill in summary, description, date, time
   - Submit and verify new workout appears in list

2. Edit existing workout via UI:
   - Click edit button (✎) on a workout
   - Modify summary in modal
   - Save and verify change reflected in list

3. Delete a workout via UI:
   - Click delete button (✕) on a workout
   - Confirm deletion in dialog
   - Verify workout removed from list

4. Pace modal save/skip flow:
   - Check a run workout checkbox
   - Pace modal opens (existing behavior)
   - Test "Save" button saves pace and closes modal
   - Test "Skip" button closes modal without saving
   - Verify pace displayed inline if saved
  </action>
  <verify>
playwright test runs pass with --grep "CRUD|Pace" filters
  </verify>
  <done>All CRUD and pace modal flows tested</done>
</task>

</tasks>

<verification>
- ICS file at frontend/public/data/trainingsplan_v2.ics exists
- E2E test file contains tests for create, edit, delete, pace-save, pace-skip
- Tests can run with mock data without Supabase connection
</verification>

<success_criteria>
- trainingsplan_v2.ics file present in frontend/public/data/
- E2E tests cover: create workout, edit workout, delete workout, pace save, pace skip
- All tests pass with mock setup
</success_criteria>

<output>
After completion, create `.planning/phases/05-gap-closure/05-gap-closure-SUMMARY.md`
</output>